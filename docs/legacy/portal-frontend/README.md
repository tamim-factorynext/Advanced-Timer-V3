# Legacy Portal Frontend

Status: Deprecated reference-only UI snapshots.

This folder preserves the previous portal pages before the frontend reset:
- `index.html`
- `config.html`
- `settings.html`

Scope:
- For historical reference and implementation inspiration only.
- Not part of active frontend development.
- Do not extend these files.

Backend note:
- V3 backend APIs remain active in firmware (`/api/v3/command`, `/api/v3/snapshot`, `/api/v3/diagnostics`).
