# Portal Layer Skeleton

Purpose: HTTP/WebSocket transport and payload translation boundaries.

Planned scope:
- route handlers
- request/response envelope mapping
- websocket publish path

Current interfaces:
- `routes.h`
